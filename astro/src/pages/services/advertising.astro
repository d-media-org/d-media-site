---
import ServiceDetailPage from "@/components/ServiceDetailPage.astro";
import { type Locale } from "@/lib/i18n";
import { getPageCopy } from "@/lib/page-copy";

const locale: Locale = "bg";
const copy = getPageCopy(locale).advertisingService;
const path = "/services/advertising";
---

<ServiceDetailPage locale={locale} path={path} copy={copy} ctaLocation="advertising_service_cta" />
